{
  "compilerOptions": {

    "typeRoots": ["./node_modules/@types"],
    "target": "ESNext",           // Совместимость с современным JS
    "module": "ESNext",           // Используется Vite/Rollup
    "moduleResolution": "bundler", // Оптимально для Vite
    "strict": true,               // Включает все strict-проверки
    "jsx": "react-jsx",           // Автоматический JSX-рантайм (не нужен импорт React)
    "esModuleInterop": true,      // Для совместимости с CommonJS
    "skipLibCheck": true,         // Ускоряет компиляцию
    "forceConsistentCasingInFileNames": true, // Проверка регистра файлов
    "baseUrl": "./",              // Для абсолютных путей (опционально)
    "paths": {                    // Алиасы путей (как в vite.config.ts)
      "@/*": ["src/*"]
    }
  },
  "include": ["src/**/*", "global.d.ts"],        // Где искать TS-файлы
  "exclude": ["node_modules"]     // Что игнорировать
}
